chrome
# launches the local chrome in the background
bundle exec jekyll serve --drafts --future
# In the foreground starts a local web server on 127.0.0.1:4000
#
# save changes and push
rake build
# copies files to lee-lindley.github.io
# cd to that repository, add, commit and push
